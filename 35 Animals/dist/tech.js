"use strict";
// creat a array.
let animals = ["Eagle", "Hawlk", "Falcon"];
//  for loop to print out the name of each animal.
for (let animal of animals) {
    console.log(animal);
}
for (let animal of animals) {
    console.log(`${animal} is a bird of pray.`);
}
// Add a line at the end, outside the for loop, that states how much I like pizza.
console.log(`They are all birds of prey.`);
